apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka-consumer

resources:
  - ../../../base/kafka-consumer
  - namespace.yaml

images:
  - name: kafka-consumer
    newTag: latest
